[package]
name = "server-module"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
spacetimedb = "2.1.0"
log = "0.4"
game-logic = { path = "../game-logic" }

[dev-dependencies]
# For unit testing trick evaluation logic without SpacetimeDB runtime
